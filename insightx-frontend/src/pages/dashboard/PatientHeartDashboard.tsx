import React, { useEffect, useState } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { jsPDF } from "jspdf";
import DashboardLayout from "../../layouts/DashboardLayout";
import HeartHero from "../../assets/researchers.png";
import { fetchPatientHeartScan } from "../../services/patientService";
import type { PatientHeartRecord } from "../../data/patientHeartData";
import { useAuth } from "../../context/AuthContext";
import { LoadingState } from "../../components/ui/LoadingState";
import { ErrorState } from "../../components/ui/ErrorState";
import { getLatestDoneSession, getSession } from "../../services/localScanStore";

const DEFAULT_PATIENT_ID = "P-0001";

const PatientHeartDashboard: React.FC = () => {
  const { user } = useAuth();
  const [searchParams] = useSearchParams();
  const [data, setData] = useState<PatientHeartRecord | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const sessionId = searchParams.get("sessionId");
  const patientId = user?.patientId ?? DEFAULT_PATIENT_ID;

  const mapSession = () => {
    const session = sessionId
      ? getSession(sessionId)
      : getLatestDoneSession(patientId, "heart");
    if (
      !session ||
      session.status !== "done" ||
      session.type !== "heart" ||
      session.patientId !== patientId ||
      !session.data?.patient
    ) {
      return null;
    }
    return session.data.patient as PatientHeartRecord;
  };

  useEffect(() => {
    setLoading(true);
    setError(null);
    const load = async () => {
      try {
        const sessionData = mapSession();
        if (sessionData) {
          setData(sessionData);
          return;
        }
        const record = await fetchPatientHeartScan(patientId);
        if (!record) setError("No heart scan data found.");
        setData(record);
      } catch (err: any) {
        setError(err?.message ?? "Unable to load heart scan.");
      } finally {
        setLoading(false);
      }
    };
    load();
  }, [patientId, sessionId]);

  const exportPdf = () => {
    if (!data) return;
    const doc = new jsPDF();
    doc.text("InsightX Diagnostic Report", 10, 20);
    doc.text(`Patient: ${data.name} (${data.patientId})`, 10, 30);
    doc.text("Scan type: Heart", 10, 38);
    doc.text(`Date: ${new Date().toISOString()}`, 10, 46);
    doc.text("Key metrics:", 10, 58);
    doc.text(`- BPM: ${data.bpm}`, 14, 66);
    doc.text(`- Oxygen: ${data.oxygen}%`, 14, 74);
    doc.text(`- Stress: ${data.stress}`, 14, 82);
    doc.text(`- Pressure: ${data.pressure}`, 14, 90);
    doc.text(`Condition: ${data.condition}`, 10, 104);
    doc.text(`Notes: ${data.doctorNotes?.[0] ?? "N/A"}`, 10, 114);
    doc.text("Generated by InsightX (mock)", 10, 130);
    doc.save(`InsightX_Patient_Heart_${data.patientId}.pdf`);
  };

  if (loading) {
    return (
      <DashboardLayout>
        <LoadingState message="Loading your heart scan..." />
      </DashboardLayout>
    );
  }

  if (error || !data) {
    return (
      <DashboardLayout>
        <ErrorState message={error ?? "No heart scan data found."} />
      </DashboardLayout>
    );
  }

  return (
    <DashboardLayout>
      <div className="space-y-4 min-w-0">
        <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
          <div>
            <h1 className="text-lg font-semibold">Your Heart Scan Overview</h1>
            <p className="text-xs text-slate-500">Scan ID: {data.scanId}</p>
          </div>
          <div className="flex items-center gap-2 flex-wrap text-xs">
            <Link
              to="/dashboard/patient/history"
              className="px-3 py-1 rounded-full border text-slate-700"
            >
              View Scan History
            </Link>
            <Link
              to="/dashboard/patient/scans"
              className="px-3 py-1 rounded-full border text-slate-700"
            >
              My Scans
            </Link>
            <button
              onClick={exportPdf}
              className="px-3 py-1 rounded-full bg-slate-900 text-white"
            >
              Export PDF Report
            </button>
          </div>
        </div>

        <div className="bg-white rounded-2xl shadow-sm border p-4 space-y-4">
          <div className="flex flex-col lg:flex-row gap-6">
            <div className="flex-1 flex items-center justify-center">
              <img src={HeartHero} className="max-h-64 object-contain" />
            </div>

            <div className="w-full lg:w-60 space-y-3 text-xs">
              <div className="bg-slate-50 p-3 rounded-xl">
                <p className="font-semibold mb-1">Heart Stats</p>
                <div className="grid grid-cols-2 text-[11px] text-slate-700 gap-1">
                  <span>BPM</span>
                  <span className="text-right">{data.bpm}</span>
                  <span>Oxygen Level</span>
                  <span className="text-right">{data.oxygen}%</span>
                  <span>Stress Level</span>
                  <span className="text-right">{data.stress}</span>
                  <span>Blood Pressure</span>
                  <span className="text-right">{data.pressure}</span>
                </div>
              </div>

              <div className="bg-slate-50 p-3 rounded-xl">
                <p className="font-semibold mb-1">Condition</p>
                <p className="text-[11px]">{data.condition}</p>
              </div>
            </div>
          </div>

          <div className="flex gap-3 text-xs">
            <button className="px-4 py-1.5 rounded-full bg-sky-700 text-white font-medium">
              3D
            </button>
            <button className="px-4 py-1.5 rounded-full bg-slate-100 text-slate-700">
              Heat map
            </button>
            <button className="px-4 py-1.5 rounded-full bg-slate-100 text-slate-700">
              Raw
            </button>
          </div>
        </div>

        <div className="bg-white rounded-2xl shadow-sm border p-4">
          <h2 className="text-sm font-semibold">Doctor Notes</h2>
          <ul className="list-disc list-inside text-[11px] text-slate-700 space-y-1 mt-2">
            {data.doctorNotes.map((note) => (
              <li key={note}>{note}</li>
            ))}
          </ul>
        </div>
      </div>
    </DashboardLayout>
  );
};

export default PatientHeartDashboard;
